# mutsume-code 設計メモ (PoC v0.2)

六角格子ベースの 2 次元コード。QR コードの構成要素（位置検出パターン / フォーマット情報 /
マスク / Reed-Solomon）を、正方格子ではなく**蜂の巣状に隣接した六角セル**の上に載せ替えたもの。

v0.2 で追加したもの:

| 項目 | 内容 |
|---|---|
| 射影変換対応 | アライメントパターン 3 個を追加し、6 点からホモグラフィを解く (`robust` プロファイル) |
| ロケータの一発識別 | 2 重リング（白中心 → 黒リング → 白リング）にして誤検出を排除 |
| 最小フォーマット | ロケータのみ・ヘッダ 3B の `micro` プロファイル (Micro QR 相当) |
| RS ブロック分割 | 符号語を複数 RS ブロックに分けてインターリーブ。255 バイト上限を撤廃 |
| 消失訂正 | 読み取りの怪しいセルを erasure として RS に渡す。訂正能力が実質 2 倍 |
| 文字モード | 数字 3.33 bit/文字、英数 5.5 bit/文字。DP で最適セグメント分割 |
| 適応的二値化 | Sauvola 法。照明ムラのある実写に対応 |
| カラーコード | 1 セル 2 bit (CMYK) / 3 bit (RGB)。容量 2〜3 倍 |
| 複数シンボル | 1 枚に写った全部を読む `decode_image_all` |
| 位置の取得 | ファインダ・外形・任意セルの画像座標を返す `Geometry` |

---

## 1. ジオメトリ

### 座標系

セルは axial 座標 `(q, r)` で表す。cube 座標は `(x, y, z) = (q, -q-r, r)`（`x+y+z = 0`）。

```
DIRS = [(1,0), (1,-1), (0,-1), (-1,0), (-1,1), (0,1)]
hex_distance((q,r)) = (|q| + |r| + |q+r|) / 2
```

- `DIRS[i] * k` = リング `k` のコーナー `i`
- `hex_ring(k)` はコーナー 0 から時計回りに `6k` セルを返す（**index `i*k` がコーナー `i`**）
- 60 度回転は `rot60(q, r) = (q + r, -q)`（コーナー `i` → `i+1`）
- 鏡映は `mirror(q, r) = (r, q)`

### セル形状

**pointy-top（尖り上）** の正六角形。中心座標は

```
x = size * √3 * (q + r/2)
y = size * 1.5 * r
```

セル同士は辺で完全に隣接する。結果としてシンボル全体の外形は **flat-top の六角形**
（左右に頂点、上下に水平な辺）になり、参照デザインと一致する。

隣接セル中心間距離 `pitch = √3 * size`（= セル幅）。

### シンボルサイズ

半径 `R`（リング数）でサイズを表す。総セル数 `3R² + 3R + 1`。対応範囲は `R = 7..40`
（`robust` は `R = 9..40`、`micro` は `R = 5..8`）。

---

## 2. マーカープロファイル

「マーカーは小さくしたい」と「射影変換に対応したい」は真っ向から相反するので、
**プロファイルとして選べる**ようにした。さらに小さい `nano`
（中央ロケータ 1 個・正対前提）は本節末尾で述べる。

| | micro | compact (既定) | robust |
|---|---|---|---|
| 半径 | 5..8 | 7..40 | 9..40 |
| ロケータ | 花形 7 セル × 3 = 21 | 同左 = 21 | 2 重リング 19 セル × 3 = 57 |
| アライメント | なし | なし | 反転パターン 7 セル × 3 = 21 |
| シグネチャ | **なし** | 6 | 9 |
| フォーマット | **なし** | 6 bit × 3 コピー = 18 | 6 bit × 1 = 6 |
| 機能セル合計 | **21** | **45** | **93** |
| ヘッダ + CRC | **3B** | 5B | 5B |
| ECC レベル | 固定 | L/M/Q/H | L/M/Q/H |
| パレット | mono 固定 | 3 種 | 3 種 |
| 姿勢 | アフィン | アフィン | + **射影（斜め撮影）** |
| ロケータ誤検出 | 三角形判定 + CRC | 三角形判定 + 一致率 + CRC | 2 重リングで**一発識別** |

機能セルは半径によらず固定数なので、相対コストはシンボルが大きいほど下がる:

```
        R=10    R=20    R=40
compact  20B    106B    449B     (ECC=M, mono, byte モード)
robust   15B    102B    445B     (-25%)  (-4%)   (-1%)
```

### micro プロファイル（最小フォーマット）

小さいシンボルでは固定オーバーヘッドが支配的になる。R=7 の compact は
データ 15B のうち、機能セル 45 + RS パリティ 4B + ヘッダ/CRC 5B が乗って
数字 9 桁しか残らない。R=6 に至っては**容量 0**（何も入らない）。

micro は Micro QR と同じ発想でここを削る:

| 削ったもの | 代わりにどうするか |
|---|---|
| シグネチャ 6 セル | 向き 6 通りを総当たりし、CRC-16 で正解を選ぶ |
| フォーマット 18 セル | ECC は半径から一意に決まる固定値。マスク 4 通りは総当たり |
| 仕様バージョン 1B | プロファイルが分かれば自明 |
| 長さ 2B | モード 2bit + 個数 6bit の 1 バイトに圧縮 |
| CRC 位置の可変性 | 符号語データ部の**末尾 2 バイトに固定配置**（長さ不要） |

復号は「半径 4 通り × 3 点対応 6 通り × マスク 4 通り」の総当たりになるが、
シンボルが小さい（≤ 217 セル、≤ 24 バイト）ので実測 0.15〜0.3 秒に収まる。

**誤受理の見積り**: 候補は最大 96 通り。ランダムな語が RS(24,18) の訂正に
成功する確率が数 % 程度、そこに CRC-16 が掛かるので
`96 × 0.05 / 65536 ≈ 7e-5`。CRC を 8 ビットに縮めると 2% 近くまで悪化するため、
**CRC-16 は削らない**。

容量（compact との比較）:

| R | セル | micro 数字/英数/B | compact 数字/英数/B |
|---:|---:|---|---|
| 5 | 91 | **7 / 4 / 3** | 不可 |
| 6 | 127 | **14 / 8 / 6** | 不可（容量 0） |
| 7 | 169 | **26 / 16 / 11** | 9 / 5 / 3 |
| 8 | 217 | **36 / 21 / 15** | 23 / 14 / 9 |

R=9 以上は compact のほうが入る（機能セルの差 24 より、ECC 選択と
セグメント分割の利得が上回る）ので、micro は R=5..8 に限定している。

### nano プロファイル（さらに小さい・中央ロケータ 1 個）

micro をさらに削り、ロケータを 3 個 → **中央ブルズアイ 1 個**（白中心 + 黒リング
= 7 セル）にした極小規格。機能セルは 7 だけ（micro の 21 の 1/3）。R=4..6。

| R | セル | 機能 | データ | 数字 / 英数 / バイト |
|---:|---:|---:|---|---|
| 4 | 61 | 7 | 6B | 2 / 1 / 1 |
| 5 | 91 | 7 | 10B | 12 / 7 / 5 |
| 6 | 127 | 7 | 15B | 19 / 11 / 8 |

ロケータが 1 個だと 3 点からアフィンを解けないので、**姿勢は相似変換**
（中心 + 一様スケール + 回転）に限られる。中心とスケールはブルズアイから取り、
向きは定まらないので**全周を ~5 度刻みで総当たりし、CRC-16 が通る角度を採る**
（micro のマスク総当たりを回転にも広げたもの）。復号の論理層は micro と共通。

- 平面内の回転・鏡映・拡大縮小・ぼかし・白黒反転には対応（実測 7/7）
- **せん断・射影（斜め撮影）には非対応**。ロケータ 1 個では異方スケールを
  解けないため。正対した画面・印刷向け
- ロケータが 1 個で手がかりが少なく、雑然とした実写では誤検出・取りこぼしに
  弱い（3 個なら三角形の相互距離で足切りできる）

### ロケータ（位置検出パターン）

compact: 白中心 + 黒リング（7 セル）。リング `R-1` のコーナー 0/2/4。

robust: **白中心 → 黒リング → 白リング**（19 セル）。リング `R-2` のコーナー 0/2/4 に
中心を置くと、外側の白リングがちょうどシンボル外縁に接する。

3 個のロケータ中心の相互距離は `3·ring·size = √3·ring·pitch` で正三角形をなす。

2 重リングにすると「1 セル大の白 → 半径 0.85·pitch の円周が黒 → 半径 1.85·pitch の円周が白」
という 3 段の条件で判定でき、データ部に偶然できる「黒 6 個に囲まれた白 1 個」を弾ける。
実測でロケータ候補が 9 個 → **真の 3 個だけ**に減った。

### アライメント（robust のみ）

**黒中心 + 白リング**（7 セル）= ロケータの反転パターン。リング `R-2` のコーナー 1/3/5。

これでロケータ 3 点 + アライメント 3 点 = **6 点**が得られ、ホモグラフィを
最小二乗で解ける。6 点は正六角形の頂点なので配置が理想的。

なお、アライメント単体（黒 1 セル + 白リング）はデータ部にも自然に現れるので、
単独では識別できない。位置はロケータから推定し、そこに引き込む形で使う（5.5 節）。

### シグネチャ（向きと半径の決定）

ロケータはコーナー 0/2/4 という 120 度対称な位置にあるため、それだけでは
回転 3 通り × 鏡映 2 通り = **6 通りの曖昧さ**が残る。そこで固定値のセルを
コーナー 1/3/5 から**時計回りにずらして**置く。

- 120 度回転 → 値の並びが巡回してパターンが変わる
- 鏡映 → ずらす向きが逆転してセル位置自体が変わる

compact は 6 セル、robust は **9 セル**（コーナーから 1,2,3 歩）。
3 セルだと偶然一致が 1/8 あり、「半径 × 対応付け」を何百通りも試す探索では
**回転した誤った姿勢が通ってしまう**（実装中に実際に踏んだ）。9 セルなら 1/512。

### フォーマット情報

6 ビット = `ECC レベル (2) | マスク ID (2) | パレット (2)`。
リング `R` 上、ロケータのコーナーを挟んだ両側（±3, ±4, ±5 歩）に置く。
compact は 120 度ずつ回した 3 か所に複製し、**ビットごとの多数決**で復元する。

3 コピーが回転対称なので、シンボルが 120 度回転していても
「どのコピーがどれか」を気にせず多数決できる。

### 描画規則（白セパレータ）

黒セルが隣接すると輪郭が融合してベタ塗りに見えるため、**隣り合う暗いセルの間には
白抜きの境界線**を引く（`render_png(..., dark_separator=True)`、既定で有効）。

| 境界 | `dark_separator` | `grid_lines` | 線 |
|---|---|---|---|
| 暗 ↔ 暗 | ● | | 白（セパレータ） |
| 明 ↔ 明 / 明 ↔ 暗 | | ● | 黒（細線） |
| 外周の明るいセル | | ● | 黒（細線） |
| 外周の暗いセル | | | なし（背景との明度差で足りる） |

2 つは独立に切れる。`grid_lines=False`（CLI では `--no-grid-lines`）にすると
**白セルの黒枠線だけが消え、黒セル同士を分ける白セパレータは残る**。
白セルが背景に溶けて、黒いセルの塊だけが浮かぶ見え方になる。

検出は「黒に囲まれた白」「白に囲まれた黒」を収縮 + 連結成分で見ているので、
輪郭線の有無に依存しない。実測でも耐性は枠線あり / なしで同一だった（7 節）。

白セパレータを先に、黒の細線を後から重ねて描き、セル頂点を黒に占有させる。
この結果、白いセルは頂点で交わる白セパレータを介して 1〜2 画素の細線で繋がるので、
デコーダ側では**マスクを収縮させてから連結成分を取る**（5.3 節）。

---

## 3. データの符号化

```
byte 0      : 仕様バージョン (0x02)
byte 1..2   : ビットストリーム長 (uint16 BE)
byte 3..    : ビットストリーム (セグメント列)
+2 バイト   : CRC-16/CCITT-FALSE (byte 0 からビットストリーム末尾まで)
残り        : パディング (0xEC, 0x11 の繰り返し)
```

### 文字モード

ビットストリームは `モード (3 bit) + 単位数 (12 bit) + データ` のセグメント列。

| モード | 符号化 | bit/文字 |
|---|---|---|
| NUMERIC | 3 桁 → 10 bit（端数 2 桁 → 7、1 桁 → 4） | 3.33 |
| ALNUM | 45 文字集合、2 文字 → 11 bit（端数 1 文字 → 6） | 5.5 |
| BYTE | 1 バイト → 8 bit | 8 |

セグメント分割は**総ビット数最小になるよう DP で探索**する。同じクラスが続く区間
(run) の境界でのみ切るので `O(runs²)`。run が多すぎる場合は切り替えコスト
（15 bit）の方が大きいのでバイトモード 1 本にまとめる。

実測: 数字 60 桁が 60B → 28B、英数 60 文字が 60B → 44B。

### Reed-Solomon とブロック分割

GF(256)（既約多項式 `0x11D`）上の RS。生成多項式の根は `α^0 .. α^(nsym-1)`。
復号は Berlekamp-Massey → Chien search → Forney。

符号語全体を **255 バイト以下のブロックに分割**し、データ部・パリティ部を
それぞれブロック横断で 1 バイトずつ**インターリーブ**して並べる。

- 255 バイトの上限がなくなり、R=40 で 609 バイト（3 ブロック）まで載る
- 連続した汚れが 1 ブロックに集中せず、全ブロックへ均等に散る

| ECC レベル | パリティ比率 |
|---|---|
| L | 15% |
| M | 25% |
| Q | 35% |
| H | 45% |

### 消失訂正 (erasure)

RS は「誤り e 個 + 位置既知の消失 f 個」を **2e + f ≤ nsym** まで訂正できる。
つまり位置が分かっていれば訂正能力は 2 倍になる。

サンプリング時に、セル平均輝度と閾値の差を「同じ値のセルの中央値」で正規化した
**確信度**を記録し、低いセルを消失候補として RS に渡す。
確信度のしきい値を 0（消失なし）→ 0.35 → 0.6 と緩めながら再試行する。

実測（中心を塗り潰したときの復号限界、画像面積比）:

```
        消失なし   消失あり
  L        3%        6%
  M        6%       15%
  Q        6%       15%
  H       10%       25%
```

### ビット配置とマスク

符号語を MSB first でビット列にし、**中心から外向きの螺旋順**（リング 0 → リング R、
各リング内はコーナー 0 から時計回り）でデータセルに詰める。1 セルにはパレットの
ビット数だけ入る。

データセルのみに `mask_value(mask_id, cell, palette)` を XOR する（機能セルには適用しない）。
カラーの場合はビット面ごとに独立なマスクを重ねる。

```
mask 0: (q + r) % 2
mask 1: (q - r) % 3 == 0
mask 2: ((q*r) % 2 + (q + s) % 3) % 2
mask 3: ((q + 2r) % 3 + s % 2) % 2      (s = -q - r)
```

4 通り全部を試し、ペナルティ（明暗の偏り + 3 方向の直線に沿った同値ランの長さ）が
最小のものを選ぶ。大きなベタ領域ができるとロケータ検出が誤爆するため。

---

## 4. パレット（白黒）と色・反転

現状は白黒（mono, 1 bit/セル）のみ。試作段階では color4（K/C/M/Y, 2 bit）や
color8（RGB 8 頂点, 3 bit）も持っていたが、印刷・撮影での色再現が読み取りに
不安定なため廃止した。palette 機構自体（フォーマット情報のパレット 2 ビット）は
将来のカラー再導入に備えて残してある。

**マーカーもデータセルも黒白で描く。** ただし生成時に暗色・明色を任意に指定でき
（`render_png(..., dark_rgb=, light_rgb=)`）、両者を入れ替えれば白黒反転になる。

読み取りは輝度ベース（Otsu / Sauvola）なので、コントラストがあれば任意の 2 色でも
復号できる。**白黒反転したコードも読める**（`decode(..., allow_inverted=True)` が既定。
二値化 sweep に反転版を 1 軸足し、反転画像はグレースケールを反転してから通常の
検出経路に流す。正常画像は通常向きで先に当たるので、探索増は未検出時のみ）。

---

## 5. デコードパイプライン

```
画像
 ├ 1. RGB / グレースケール化
 ├ 2. 作業サイズ x 二値化を安い順に総当たり (5.7)
 ├ 3. マスクを 0/1/2 画素ぶん収縮 → 連結成分ラベリング (5.3)
 ├ 4. マーカー候補
 │     ロケータ  : 1 セル大の白 + 半径 0.85*pitch が黒 (+ 1.85*pitch が白なら 2 重リング)
 │     アライメント: 1 セル大の黒 + 半径 0.85*pitch が白    ※ robust のみ
 ├ 5. ロケータ 3 点が正三角形をなす組を列挙 (5.7)
 ├ 6. 姿勢候補を逐次生成し、確からしいものから復号を試す (5.4)
 ├ 7. 全セルをサンプリング (射影に応じてセルごとに標本半径を決める)
 └ 8. フォーマット読み取り → パレット判定 → マスク解除 → RS 復号 → CRC 検証
      失敗したら確信度の低いセルを消失にして再試行
```

### 5.3 マスクの収縮（細線の切断）

セパレータや輪郭の細線はセル頂点で交わって網目状に繋がる。そのため素朴に連結成分を
取ると、マーカー中心のセルまで背景と一体化してしまう。

そこでマスクを 8 近傍で 0 / 1 / 2 画素ぶん収縮させ、それぞれで連結成分を取る。
1〜2 画素幅の線は切断され、セル内部（半径 0.5·pitch 程度）は残る。

- 収縮量は表示サイズで必要量が変わるので、複数試して統合する
- 収縮で痩せたぶんは `pitch = 外接幅 + 2k` で補正する
- 円周判定は収縮したマスクの補集合（= 細線を埋めたマスク）に対して行う
- ロケータはある収縮量で正三角形の組が見つかった時点で打ち切る

### 5.4 半径と姿勢の決定

ロケータ 3 点の相互距離とセルピッチから半径 `r_est` を推定し、その近傍
（`± max(3, 20%)`）だけを試す。三つ組が正しければ `r_est` はよく当たり、
正しくなければ何を試しても通らないため、全半径を試す意味は薄い。

姿勢候補は**逐次生成**する。「シグネチャ完全一致 + マーカーほぼ一致」の姿勢が
出た時点で復号まで試し、通ればそこで終わる。外れたら生成を続けるので
**最悪ケースは総当たりのまま**で堅牢性は落ちない。

「ロケータ花形は R が 1 ずれても当たってしまう」（スケール誤差 ~9% ではサンプル点が
まだ正しいセル内に落ちる）ので、順位付けはシグネチャを重く見る
（`0.35 × マーカー + 0.65 × シグネチャ`）。均等配分にすると正しい姿勢が上位から溢れる。

### 5.5 ホモグラフィの推定（robust）

1. ロケータ 3 点からアフィン変換を解く
2. アフィンでアライメント位置を予測し、検出済みの黒ブロブを近い順に割り当てる
3. 6 点でホモグラフィを解く
4. **解いたホモグラフィで予測し直して割り当てをやり直す**（3 回まで反復）
5. 機能セル一致率 + シグネチャ完全一致で採否を決める

4 の反復が要点。アフィン予測は射影歪みで 1〜2 セルずれるので初回の割り当ては外すが、
1 個でも当たればホモグラフィが改善し、次の周回で残りも正確になる。

アライメントが blob として取れない場合は、予測位置まわりの格子探索
（マーカーらしさ = リングと中心の明暗差）＋ 塗り領域の重心によるサブピクセル補正、
という反復にフォールバックする。

### 5.6 姿勢の外部提供 (Geometry)

復号に使ったホモグラフィは捨てずに `DecodeResult.geometry` として返す。
これで「ファインダはどこか」「シンボルの外形はどこか」「任意のセルはどの画素か」を
すべて同じ 1 つの行列から答えられる。

```
Geometry
  homography   3x3 : モデル座標 -> 入力画像座標
  finders      ファインダ中心 x3
  alignments   アライメント中心 x3 (robust)
  outline      外形の 6 頂点 (リング R のコーナーセルの外側の頂点)
  corner0      コーナー 0 = シンボルの「正面」の頂点 (向きの指標)
  mirrored     鏡映に見えているか (中心でのヤコビアンの向きで判定)
  center / bbox / cell_size / rotation_deg / perspective
  cell_center(cell) / cell_polygon(cell) / cell_centers()
```

向きは常に一意に決まる。ロケータ配置は 120 度対称だが、シグネチャ
(micro は CRC 総当たり) が 6 回転 x 鏡映の曖昧さを解いた上でホモグラフィが
確定するため、`rotation_deg` / `corner0` は「シンボルのどの辺が上か」まで表す。

注意点が 1 つある。デコーダは画像を縮小して処理するので、そのままだと座標が
作業画像のスケールになる。`build_geometry` で

```
H_full = diag(1/s, 1/s, 1) @ H
```

と縮小率を打ち消し、**常に入力画像そのままの座標**で返す。

外形は「リング R のコーナーセルの、さらに外側の頂点」を使う。
モデル座標では中心から `(R + 0.5) / R` 倍の位置に当たるので、
セル半径ぶんを正確に含んだ六角形になる。

実測精度（レンダリング時の既知座標との誤差）:

| 条件 | 最大誤差 |
|---|---|
| 等倍 | 0.6〜0.9 px |
| 縮小 x0.5 | 0.9〜1.2 px |
| 拡大 x2（内部縮小あり） | 0.8〜1.7 px |

### 5.7 実写での検出（クラッタ対策）

ノート PC の画面を手持ちで撮った実写映像 (1920x1080) では、
当初 10.5% のフレームでしか読めなかった。原因は 2 つとも**探索の優先順位**で、
シンボル自体は切り出せば問題なく読めていた。

**(a) ロケータ三つ組の足切り** — 画面を撮ると UI の白い部分が大量に
ロケータ候補になる (平均 36 個)。三つ組は「大きい順」に並べていたため、
散らばった偽候補が作る巨大な三角形が上位を占め、真のシンボル
(辺長 400px 程度) が上限に入らなかった。

ロケータ 3 点の相互距離は必ず `√3 * ring * pitch` なので、
`辺長 / (√3 * pitch)` がありうるリング数の範囲に入らない組は捨てる。
実測では偽の組が リング数 27〜113 を示しており、きれいに分離できた。

順位付けは 2 基準の上位を混ぜる。リング数が整数に近い順 (小さいシンボルで有効) と、
三角形が大きい順 (リング数が大きいとピッチ誤差で整数判定が効かないので必要)。
片方に寄せると、実写かレンダリング画像のどちらかで取りこぼす。

**(b) 作業画像サイズの階段** — 縮小すると UI の細部やモアレが平滑化されて
ノイズ候補が激減し、**検出率が上がる**。

| 作業サイズ | 1400px | 900px | 600px | 500px | 420px |
|---|---:|---:|---:|---:|---:|
| 検出率 | 24% | 30% | 66% | 83% | **91%** |

小さい方から順に試す (小さい方が安いので当たれば速い)。ただし座標の量子化が
粗くなるので、当たったら判明した半径・プロファイルで最大サイズを引き直し、
同じ内容が読めればそちらのジオメトリを採る (粗く検出 → 精密化)。

(二値化の種類, 作業サイズ) の組は **安い順** に並べて試す。
コストはおおむね「画素数 × 二値化の重み」。種類を外側にすると
otsu@1400 (高い) を sauvola@420 (安い) より先に払うことになる。
どちらで当たるかは画像次第 (実測 otsu 75 / sauvola 30) なので、
全部試すのは変えずに、順序だけ変えて当たるまでの時間を縮める。

結果: 1 つ以上を検出 10.5% → **99.0%**、2 つとも検出 0% → **42.1%**。

### 5.8 複数シンボル

`decode_image_all` は「読めたコードの外形を除外領域に足して探し直す」を繰り返す。
除外はロケータ候補の段階で効かせるので、同じシンボルを二度返さない。

2 つめ以降も条件を絞らずに探索し直す。1 枚に写ったコードは大きさも見え方も違い、
当たる作業サイズ・二値化がそれぞれ違うため:

| 2 つめの探索条件 | 2 つとも検出 |
|---|---:|
| 条件を絞らない | **42.1%** |
| 1 つめと同じサイズに固定 | 5.3% |
| 1 つめと同じ二値化に固定 | 9.2% |

そのかわり二値化とロケータ候補の抽出は (種類, サイズ) ごとに 1 回だけ行って
キャッシュする。ここが検出コストの大半なので、2 個目以降の探索と
「もう無い」の確認がほぼ姿勢探索だけで済む。

### 5.9 フレーム時間のばらつき

実写映像を 1 フレームずつ復号したときの内訳 (作業幅 600px, 150 フレーム):

| 検出数 | フレーム数 | 中央値 | 構築した文脈数 |
|---|---:|---:|---:|
| 2 個 | 56 | **62 ms** | 3.1 |
| 1 個 | 91 | **232 ms** | 6.0 |
| 0 個 | 3 | 415 ms | 6.0 |

**遅いのは「見つからなかったとき」**で、速いフレームの 4 倍かかる。
探しているものが在れば早い段階で当たって打ち切れるが、無いことを確かめるには
(二値化の種類 × 作業サイズ) の組み合わせを全部試し切るしかないため。
内訳でも、遅いフレームは姿勢探索 + 復号が 175 ms (全体の 6 割) を占め、
候補数も 43 → 102 と倍以上になっている (画面の雑然さがそのまま効く)。

見つからないフレーム 1 枚で実際に走る量 (計装して数えたもの):

| 処理 | 回数 | 時間 |
|---|---:|---:|
| 二値化 | 6 | 47 ms |
| ロケータ検出 (収縮 + ラベリング) | 6 | 33 ms |
| 三つ組の列挙 | 24 回 / 1.7 万組 | 8 ms |
| 姿勢評価 (機能セル照合) | 2636 | 40 ms |
| セルのサンプリング | 33 | 15 ms |
| RS 復号の試行 | 48 | 54 ms |

見つかるフレームではこれが 1/3 以下 (二値化 3.1 回 / 復号試行 17 回) で済む。

**この総当たり自体は必要**で、これが検出率 10.5% → 99% を支えている。
実際、`profile="compact"` に固定するとむしろ **233 ms と遅くなる**
(micro のシンボルが永久に見つからず、毎回全部を試し切るため)。
自動判別 170 ms より遅いのは、失敗のほうが成功より高いことの裏返し。

削れたのは重複だけだった:

- 三つ組の列挙は候補数の 3 乗。compact と micro が同じ引数で 2 回呼んでいたので
  1 回にした (31.6 → 24.4 回、2.4 万組 → 1.7 万組)
- スコアの低い姿勢に復号までさせない下限 (`POSE_TRY_MIN`) を入れた
  (姿勢が外れるとシグネチャが崩れてスコアが落ちる)

つまり **1 枚に写っている枚数ちょうどを `max_symbols` に指定できるかどうか**が
一番効く。指定できない場合のために、2 つめ以降の探索には上限
(`EXTRA_SYMBOL_BUDGET` = 三つ組 6 組 / 姿勢 6 回) を設けてある。
もう 1 つ写っているなら真の三つ組は上位に来るので取りこぼしは少ない。

| 2 つめの探索上限 | のべ検出数 | 2 つ検出 | 平均 |
|---|---:|---:|---:|
| 無制限 | 240 | 58 | 242 ms |
| **(6, 6)** | **203** | **56** | **182 ms** |
| (4, 4) | 197 | 50 | 165 ms |
| (2, 2) | 183 | 36 | 151 ms |

(6, 6) より絞ると、速度はほとんど変わらないのに検出数だけ落ちる。

### 5.10 速度

レンダリング画像 1 枚あたり（Python + numpy、プロファイル自動判別）:

| | 当初 | 現在 |
|---|---:|---:|
| compact シンボル | 594 ms | **29 ms** |
| robust シンボル | 約 800 ms | **44 ms** |
| micro シンボル | - | **7 ms** |

実写 (1920x1080):

| | 当初 | 現在 |
|---|---:|---:|
| 1 コード検出 (フル解像度) | 874 ms | **270 ms** |
| 2 コード検出 (作業幅 600px) | 2918 ms | **110 ms** |

効いた順に:

1. **姿勢候補の遅延生成と早期復号**（最大）。半径 × 3 点対応の総当たりは
   最大 200 通り超あり、全部を評価してから順位付けしていた。
   確からしい姿勢が出た時点で復号まで試し、通ればそこで終わる。
2. **機能セル照合のベクトル化**。姿勢 1 つにつき 45〜93 セルを 1 つずつ射影して
   いた（1 フレームで `project` が約 8 万回）。モデル座標と期待値を
   `lru_cache` した numpy 配列にまとめ、一括射影 + 一括比較にした。
3. **Sauvola の遅延評価**。`(otsu, *(sauvola...))` とタプルに展開していたため、
   Otsu が通っても毎回 Sauvola を 2 回（各約 50 ms）計算していた。
4. **アライメント探索の早期打ち切り**（robust）。候補が 10 個集まった時点で止める。
5. **ラベリング時の 1 画素ランの除去**。探しているのはセル大の塊なので影響がない。
6. **半径の絞り込み**（1873 → 1282 ms）。compact は半径 7..40 を総当たりして
   いたが、`r_est ± max(3, 20%)` に絞る。雑然とした実写では偽の三つ組が
   大量に出るため、姿勢評価が 1 フレーム 3 万回まで膨らんでいた。
7. **マスク値のキャッシュ**。`decode_grid` はデータセルごとにマスクを計算し直して
   おり、姿勢候補ぶん繰り返すと 1 フレーム 700 万回に達していた。
8. **消失訂正の再試行を上位候補だけに**。姿勢が外れている候補ではどの消失集合でも
   通らないのに、RS 復号を 3 倍払っていた。
9. **RGB サンプリングの遅延評価**。カラーパレットのときしか要らない。
10. **(二値化, サイズ) をコスト順に並べる**（1184 → 1024 ms）。
11. **連結成分ラベリングの numpy 化**（単体で 3.0〜3.6 倍）。
    行ごとに `np.diff` を呼ぶと numpy の呼び出しコストを行数ぶん払っていた
    (1400 行で 20ms 超)。全行のランを 2 次元のまま 1 回で抽出し、
    上下の隣接判定も `searchsorted` で一括にした。
    ランは行優先ソート済みなので、行 y のランと重なる行 y-1 のランは
    **連続した区間**になり、その両端を二分探索で求められる (総当たりの
    二重ループが消える)。集計も `argsort` + `reduceat` で一括。
    Python のループは union-find の統合だけに残した (組の数はラン数程度で元々安い)。
    ランダムマスク 60 通り + 端のケースで旧実装と完全一致することを確認済み。
12. **セルのモデル座標のキャッシュ**。サンプリングのたびに作り直しており、
    `to_cartesian` が 1 フレーム 480 万回に達していた。
13. **RS 症候群と Chien search の numpy 化**。失敗する復号試行が多い実写では
    `gf_mul` が 1 フレーム 26 万回に達していた。gf_mul は exp/log テーブルで
    表せるので、非ゼロ係数だけを一括評価する (小さい符号語は numpy 呼び出しの
    方が高いのでスカラーへフォールバック)。ランダム試験 6500 件で検証。
14. **姿勢照合の融合**。マーカーとシグネチャを別々に射影していた
    (1 フレーム 2000 回 x 2)。1 本の配列にまとめ、同次座標までキャッシュして
    1 回の行列積で両方の一致率を出す。
15. **ビット詰めの packbits 化**。復号試行ごとにセル単位の Python ループで
    バイトへ詰めていたのを、マスク XOR ごと numpy で一括に。
16. **union-find を Python list で**。ndarray の要素アクセスは 1 回ごとに
    int 変換が入り、200 万回のループでは list の 3 倍近く遅い。find も
    インライン展開した。
17. **Sauvola の箱和をスライス演算に**。clip した添字での fancy indexing は
    8 回の全画素 gather になる。画像を半径ぶんのゼロで囲ってから積分すると、
    端の切り詰めが「はみ出し = 0」になり純粋なスライスだけで書ける。
18. **円周サンプリングと三つ組列挙と色分類のバッチ化**。いずれも
    「候補ごとに小さい処理を Python で回す」形だったものを一括評価に。

コストは平坦になった。単一の大きなボトルネックは残っていない。
**間引いたマスクで粗く探す案は棄却した**: 1.3 倍速くなる一方で、
ぼかし・回転時に真のロケータを取りこぼしつつ「それらしい三つ組」はできてしまい、
復号率が落ちた（テスト 3 件が退行）。

### 5.11 動画のトラッキング

1 フレームごとにゼロから探すのは動画では無駄が大きい。`decode_image_all` の
`hints` に前フレームの `DecodeResult.geometry` を渡すと、検出を跳ばして
その姿勢の近傍だけで復号を試す:

1. 前回のホモグラフィでファインダ 3 点の位置を予測する
2. 各点を予測位置の近く (±1 セルピッチ) で取り直す (`refine_marker`)
3. 3 点でアフィンを解き直し、マーカー一致率で検証してから復号する

当たれば **二値化 + サンプリング + 復号だけ** で済み、ロケータ検出
(ラベリング)・三つ組列挙・姿勢総当たりを丸ごと省ける。追従できなかった
シンボルぶんは通常の全探索に落ちる。新しく現れたシンボルはヒントでは
見つからないので、呼び出し側が定期的に全探索フレームを挟む
(デモは `--refresh`、既定 10 フレームごと)。

実写映像を連続フレームで処理した実測 (作業幅 700px):

| | 検出フレーム | のべ検出 | 平均 |
|---|---:|---:|---:|
| トラッキングなし | 98.0% | 243 | 105 ms |
| トラッキングあり (既定) | **100%** | 283 | **13 ms** |

速くなるだけでなく**検出率も上がる**。全探索が失敗するフレーム (ブレ・モアレ)
でも、前フレームの姿勢からの追従は通ることが多いため。フレーム間の動きが
±1 セルピッチを超える速い動きでは追従が切れるが、その場合も全探索に
落ちるだけで、静止画と同じ検出率は保たれる。

---

## 6. 容量

`python -m mutsume info` で確認できる（ペイロード上限バイト数 / ECC=M）:

```
       |   compact    | robust
     R | mono  数字   | mono
    10 |    20    49   |    15
    14 |    47   114   |    43
    20 |   106   256   |   102
    30 |   249   599   |   245
    40 |   449  1079   |   445
```

---

## 7. 実測した耐性

| 条件 | compact/mono 枠線あり | compact/mono 枠線なし | robust/mono 枠線なし |
|---|---|---|---|
| 任意角度の回転・鏡映 | OK | OK | OK |
| 縮小 22%（1 セル約 7px） | OK | OK | OK |
| ぼかし σ=2 / ノイズ σ=25 / JPEG q35 | OK | OK | OK |
| せん断 0.2 | OK | OK | OK |
| **射影 傾き 8% / 15%** | **不可** | **不可** | **OK** |
| 照明ムラ（左端 0.35 / 0.15 倍） | OK | OK | OK |
| 合計 | 12/12 | 12/12 | **12/12** |

白セルの黒枠線を消しても（`grid_lines=False`）耐性は変わらない。
射影歪みに耐えるかどうかはプロファイルだけで決まる。

中心を円で塗り潰したときの復号可否（% は画像面積比、括弧内は消失訂正のバイト数）:

```
  ecc |  R |   6% |  10% |  15% |  20% |  25%
   L  | 12 | ( 8) |   x  |   x  |   x  |   x
   M  | 13 | ( 0) | (12) | (16) |   x  |   x
   Q  | 14 | ( 0) | ( 0) | (21) |   x  |   x
   H  | 15 | ( 0) | ( 0) | ( 4) | (28) | (34)
```

限界は素直に RS の訂正能力 `t = nsym/2` バイトで決まる。

---

## 8. 既知の限界 / 今後の拡張

- **射影歪みは傾き 15% 程度まで**。それ以上（視野角 45 度超）ではマーカーが潰れて
  検出できなくなる。奥側のセルが 20px を切ると厳しい。
- **`compact` / `micro` は射影変換に対応しない**（設計上の割り切り）。
  斜めから撮る用途なら `robust` を選ぶ。
- **カラーは印刷・撮影条件に敏感**。実測はレンダリング画像に対するもので、
  実際の印刷 → 撮影では色域変換の検証が要る。
- **消失位置の推定は輝度マージンのみ**。遮蔽領域を面として検出できれば
  もっと積極的に消失を宣言できる。
- **「もう無い」の確認が高い**。複数シンボル探索では、実際に写っている枚数を
  `max_symbols` に指定できるかどうかで速度が数倍変わる（5.9 節）。
- 二値化は大域 Otsu と Sauvola の 2 段。極端な影・反射には未対応。
